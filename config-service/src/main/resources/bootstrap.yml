server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NVFox/app_microservicios.git
          searchPaths: config-data
          default-label: master
          username: ${GIT_USER}
          password: ${GIT_PASS}
